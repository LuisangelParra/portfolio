---
import ProjectCard from "./ProjectCard.astro";

const { projects } = Astro.props;
---

<ul class="mt-8 sm:mt-12 grid grid-cols-1 lg:grid-cols-2 xl:grid-cols-3 gap-12">
  {projects.map((project: object) => (
    <ProjectCard project={project} />
  ))}
</ul>
